let initialState = {
    friendsList: [
      { id: 1, name: "Dima" },
      { id: 2, name: "Boris" },
      { id: 3, name: "Diana" },
    ],
  };

const sidebarReducer = (state = initialState, action) => {
    

    return state;
}

export default sidebarReducer; 